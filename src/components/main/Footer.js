import classes from './Footer.module.css';

const Footer =()=> {
    return (
        <footer className={classes.footer}>
            <p className={classes.footer_p}>www.e-shop.pl</p>
        </footer>
    )
}
export default Footer;