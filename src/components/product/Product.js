import { useParams } from 'react-router-dom';
import { classes } from './Product.module.css';

const Product =()=> {
    // const {id} = useParams()
    return(
        <div>
        </div>
    )
}
export default Product;